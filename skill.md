# Rikkahub Skills

> 移动端聊天+工作 Agent 技能包。通过 DeepSeek API 驱动。

---

## 1. planning-and-task-breakdown

将工作拆分为有序任务。当你有了需求规范或明确需求后，需要将工作分解为可执行的任务时使用。

### 工作流程
1. **进入规划模式** — 只读，不写代码
2. **识别依赖关系图** — 绘制模块间依赖
3. **垂直切片** — 每个任务是一条完整的功能路径（DB + API + UI），不要水平切
4. **写任务** — 包含描述、验收标准、验证步骤、依赖、涉及文件、估算范围
5. **排序与检查点** — 每 2-3 个任务设置验证检查点

### 任务大小指南
- XS: 1 个文件
- S: 1-2 个文件（一个组件或端点）
- M: 3-5 个文件（一个功能切片）
- L: 5-8 个文件（考虑拆分）
- XL: 8+ 个文件（必须拆分）

---

## 2. search-first

编写代码前先研究。在写自定义代码之前搜索已有工具、库和模式。

### 决策矩阵
| 情况 | 操作 |
|------|------|
| 精确匹配、维护良好、MIT/Apache 许可 | **采用** — 直接安装使用 |
| 部分匹配、基础良好 | **扩展** — 安装 + 写薄封装 |
| 多个弱匹配 | **组合** — 组合 2-3 个小包 |
| 无合适方案 | **自建** — 基于研究写定制代码 |

### 反模式
- 不搜索就直接写代码
- 忽略已有的 MCP/工具
- 过度封装一个库
- 为一个功能引入巨大依赖

---

## 3. incremental-implementation

增量交付变更。每次只实现一个最小的完整功能片段，测试、验证、提交，然后继续。

### 增量循环
```
实现 → 测试 → 验证 → 提交 → 下一片
```

### 核心规则
- **简单优先**: 能用三行代码就别上抽象
- **范围纪律**: 别顺手改无关代码
- **一次一件事**: 别在一个 commit 里混入重构和功能
- **保持可编译**: 每片之后项目必须能构建
- **安全默认**: 新功能默认关闭

---

## 4. verification-loop

完成功能或代码变更后的综合验证。

### 验证阶段
1. 构建验证
2. 类型检查
3. Lint 检查
4. 测试套件
5. 安全扫描（密钥泄露、console.log 等）
6. Diff 审查

### 输出格式
```
VERIFICATION REPORT
Build:     [PASS/FAIL]
Types:     [PASS/FAIL]
Lint:      [PASS/FAIL]
Tests:     [PASS/FAIL]
Security:  [PASS/FAIL]
Overall:   [READY/NOT READY]
```

---

## 5. cost-aware-llm-pipeline

LLM API 调用成本优化。结合模型路由、预算追踪、重试逻辑和提示缓存。

### 核心技巧
- **按任务复杂度路由模型**: 简单任务用便宜模型（Haiku），复杂任务才用贵的（Sonnet）
- **不可变成本追踪**: 每次 API 调用返回新的追踪器，不修改状态
- **窄重试逻辑**: 只在网络错误、限流、服务端错误时重试，认证/参数错误直接失败
- **提示缓存**: 长系统提示用 `cache_control` 缓存

### DeepSeek 定价参考（类比）
- 简单任务 → 用便宜模型（约 1/4 成本）
- 复杂任务 → 用标准模型
- 始终设置预算上限，超支即停

---

## 6. frontend-ui-engineering

构建生产级 UI。用于移动端界面组件开发。

### 组件原则
- **组合优于配置**: 用 `<Card><CardHeader>...</CardHeader></Card>` 而非 `<Card title="..." headerVariant="large" />`
- **数据与展示分离**: Container 处理数据，Presentation 处理渲染
- **状态管理选择**:
  - 本地 state → 组件内 UI 状态
  - 提升 state → 2-3 个兄弟组件共享
  - Context → 主题、认证、语言
  - URL state → 筛选、分页

### 移动端特有
- **触摸目标**: 最小 44x44pt
- **安全区域**: 适配刘海屏/底部指示条
- **键盘处理**: 输入框不被键盘遮挡
- **手势**: 滑动删除、下拉刷新

### 反 AI 审美
不使用紫/靛蓝渐变、过大的圆角、过度阴影、统一样式的卡片网格

---

## 7. frontend-design

为前端建立有意的视觉方向，通过调色板、排版、结构和纹理来保持。

### 八个视觉锚点
1. **Swiss** — 纯白 + 无衬线 + 网格线 + 一种强调色
2. **Industrial** — 纯黑 + 等宽字体 + 语义色（绿/红/琥珀）
3. **Brutalist** — 纯原色 + 系统字体 + 硬阴影
4. **Aurora Maximalism** — 暗色渐变 + 霓虹发光 + 弹簧动画
5. **Chaotic Maximalism** — 冲突配色 + 混合字体 + 满铺图案
6. **Retro-Futuristic** — 像素/VT323 + 霓虹发光 + CRT 扫描线
7. **Organic** — 大地色 + 人文衬线 + 圆角 + 纹理
8. **Lo-Fi** — 纸黄 + 混合系统字体 + 旋转元素 + 网点过渡

### 内容纪律
- 禁止伪造数据、填充标签、Unicode 符号代替图标
- 标准操作用标准文案（"登录"不写"验证会话"）

---

## 8. code-review-and-quality

多维度代码审查。合并前必做。

### 五轴审查
1. **正确性** — 代码是否做对了？边界情况？错误路径？
2. **可读性** — 命名清晰？控制流直接？没有死代码？
3. **架构** — 符合现有模式？模块边界清晰？无循环依赖？
4. **安全性** — 输入验证？无密钥泄露？SQL 参数化？XSS 防护？
5. **性能** — 无 N+1 查询？无无界循环？有分页？

### 审查意见标注
- 无前缀 → 必须修改
- **Critical:** → 阻塞合并（安全漏洞、数据丢失）
- **Nit:** → 可选，作者可忽略
- **Optional:** → 建议但不强制

---

## 9. security-review

安全检查清单和模式。涉及认证、用户输入、密钥、API 端点时使用。

### 核心清单
1. **密钥管理** — 环境变量，绝不硬编码
2. **输入验证** — Schema 校验，白名单非黑名单
3. **SQL 注入防护** — 参数化查询
4. **认证授权** — JWT 存 httpOnly cookie，操作前验证权限
5. **XSS 防护** — 用户内容净化，CSP 头
6. **CSRF 防护** — Token + SameSite Cookie
7. **限流** — 所有 API 端点
8. **敏感数据** — 不在日志/错误消息中暴露
9. **依赖安全** — 定期 npm audit
10. **移动端额外关注** — API key 安全存储（Keychain/Keystore），传输加密

---

## 使用说明

在 Rikkahub 中根据当前任务类型切换对应的 skill 模式：
- 开始新功能 → `planning-and-task-breakdown` + `search-first`
- 正在写代码 → `incremental-implementation`
- 代码写完 → `verification-loop`
- 写 UI → `frontend-ui-engineering` + `frontend-design`
- 合并前 → `code-review-and-quality`
- 涉及安全 → `security-review`
- 控制成本 → `cost-aware-llm-pipeline`
